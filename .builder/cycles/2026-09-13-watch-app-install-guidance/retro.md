# Retro

- Belief: a primary action beats passive instructions when the next step lives in another app.
- Learning: the guide already detected `appNotInstalled`; only the action was missing, so the fix stayed small.
- Decision: close the cycle and verify the `bridge://` open path on a real paired device.
- Follow-up: revisit only if device testing shows the Watch app does not open or lands somewhere confusing.
