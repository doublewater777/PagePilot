# Experiments

- Verify the `appNotInstalled` guide state renders an install action that opens the Watch app URL, via source-boundary tests.
- Verify the new `onboarding_watch_install_action` key is localized in all five supported languages.
- Run the focused tests, then the full iPhone 17 simulator suite and the iPad mini build.
- Loop back if device testing shows `bridge://` is blocked or lands somewhere confusing.
