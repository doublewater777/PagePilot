# Retro

- Belief: reminders about missing hardware belong to device state, not to a one-time flow.
- Learning: the onboarding gate made the install guidance invisible to exactly the users who needed it most — everyone past first run.
- Decision: close the cycle and verify the Reader tip and Watch Settings row on a paired device without the Watch app installed.
- Follow-up: revisit only if the recurring tip reads as nagging once the Watch app is deliberately not installed.
