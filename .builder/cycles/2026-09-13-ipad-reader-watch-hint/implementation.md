# Implementation

- Update `onboarding_ipad_reader_hint_title` and `onboarding_ipad_reader_hint_detail` in English, Simplified Chinese, German, Spanish, and French.
- Keep the existing `OnboardingIPadReaderHintView` and dismissal behavior unchanged.
- Add a localization regression test using the built app bundles.
